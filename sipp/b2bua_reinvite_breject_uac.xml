<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!--
  B2BUA B-leg re-INVITE rejected UAC scenario (A-leg side):
  INVITE → 200 → ACK → receive re-INVITE → 488
                       → BYE → 200

  The UAC (A-leg) receives a re-INVITE forwarded from the B-leg UAS
  and rejects it with 488. Tests that B2BUA forwards the 488 back
  to the UAS AND ACKs the UAC for the 488.
  Call must survive after rejected re-INVITE.
-->
<scenario name="B2BUA B-leg re-INVITE reject UAC">

  <!-- Send initial INVITE to B2BUA -->
  <send retrans="500">
    <![CDATA[
INVITE sip:bob@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:alice@[local_ip]>;tag=[pid]SIPpTag00[call_number]
To: <sip:bob@[remote_ip]>
Call-ID: [call_id]
CSeq: 1 INVITE
Contact: <sip:alice@[local_ip]:[local_port];transport=[transport]>
Max-Forwards: 70
User-Agent: SIPp/b2bua-breject-test
Content-Type: application/sdp
Content-Length: [len]

v=0
o=alice 53655765 2353687637 IN IP[local_ip_type] [local_ip]
s=-
c=IN IP[media_ip_type] [media_ip]
t=0 0
m=audio [media_port] RTP/AVP 0 8 101
a=rtpmap:0 PCMU/8000
a=rtpmap:8 PCMA/8000
a=rtpmap:101 telephone-event/8000
a=fmtp:101 0-15
a=sendrecv
]]>
  </send>

  <!-- Absorb provisionals -->
  <recv response="100" optional="true" />

  <label id="1" />
  <recv response="180" optional="true" next="1" />

  <!-- 200 OK for initial INVITE -->
  <recv response="200" rtd="true" crlf="true" />

  <!-- ACK for initial INVITE -->
  <send>
    <![CDATA[
ACK sip:bob@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:alice@[local_ip]>;tag=[pid]SIPpTag00[call_number]
To: <sip:bob@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 1 ACK
Max-Forwards: 70
Content-Length: 0

]]>
  </send>

  <!-- ============================================================ -->
  <!-- Receive re-INVITE from B2BUA (forwarded from B-leg UAS) -->
  <!-- Reject with 488 Not Acceptable Here -->
  <!-- ============================================================ -->
  <recv request="INVITE" crlf="true" />

  <!-- Send 488 Not Acceptable Here -->
  <send retrans="500">
    <![CDATA[
SIP/2.0 488 Not Acceptable Here
[last_Via:]
[last_From:]
[last_To:]
[last_Call-ID:]
[last_CSeq:]
Content-Length: 0

]]>
  </send>

  <!-- ACK from B2BUA for 488 (hop-by-hop) -->
  <!-- This is mandatory: if Siphon doesn't ACK, test times out -->
  <recv request="ACK" timeout="5000" />

  <pause milliseconds="200" />

  <!-- ============================================================ -->
  <!-- BYE — call must survive after rejected re-INVITE -->
  <!-- ============================================================ -->
  <send retrans="500">
    <![CDATA[
BYE sip:bob@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:alice@[local_ip]>;tag=[pid]SIPpTag00[call_number]
To: <sip:bob@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 2 BYE
Max-Forwards: 70
Content-Length: 0

]]>
  </send>

  <!-- 200 OK for BYE -->
  <recv response="200" rtd="true" />

</scenario>
