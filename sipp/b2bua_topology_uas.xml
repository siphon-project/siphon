<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!--
  B2BUA topology hiding UAS scenario (B-leg side):
  Validates that the full INVITE -> 180 -> 200 -> ACK -> re-INVITE -> 200 ->
  ACK -> BYE -> 200 flow completes when the UAC uses non-standard CSeq (99)
  and Max-Forwards (65). The B2BUA must regenerate CSeq and decrement
  Max-Forwards for the flow to succeed.

  Header-level topology hiding assertions are covered by Rust unit and
  integration tests (rewrite_uri_host, sanitize_sdp_identity, CSeq
  independence, etc.).
-->
<scenario name="B2BUA topology hiding UAS">

  <!-- Receive initial INVITE from B2BUA (B-leg) -->
  <recv request="INVITE" crlf="true" />

  <!-- Send 180 Ringing -->
  <send>
    <![CDATA[
SIP/2.0 180 Ringing
[last_Via:]
[last_From:]
[last_To:];tag=[pid]SIPpTag01[call_number]
[last_Call-ID:]
[last_CSeq:]
Content-Length: 0

]]>
  </send>

  <pause milliseconds="200" />

  <!-- Send 200 OK with SDP -->
  <send retrans="500">
    <![CDATA[
SIP/2.0 200 OK
[last_Via:]
[last_From:]
[last_To:];tag=[pid]SIPpTag01[call_number]
[last_Call-ID:]
[last_CSeq:]
Contact: <sip:bob@[local_ip]:[local_port];transport=[transport]>
Content-Type: application/sdp
Content-Length: [len]

v=0
o=bob 53655765 2353687637 IN IP[local_ip_type] [local_ip]
s=-
c=IN IP[media_ip_type] [media_ip]
t=0 0
m=audio [media_port] RTP/AVP 0 8 101
a=rtpmap:0 PCMU/8000
a=rtpmap:8 PCMA/8000
a=rtpmap:101 telephone-event/8000
a=fmtp:101 0-15
a=sendrecv
]]>
  </send>

  <!-- Receive ACK -->
  <recv request="ACK" optional="true" timeout="5000" />

  <!-- Receive re-INVITE -->
  <recv request="INVITE" crlf="true" />

  <!-- 200 OK for re-INVITE -->
  <send retrans="500">
    <![CDATA[
SIP/2.0 200 OK
[last_Via:]
[last_From:]
[last_To:];tag=[pid]SIPpTag01[call_number]
[last_Call-ID:]
[last_CSeq:]
Contact: <sip:bob@[local_ip]:[local_port];transport=[transport]>
Content-Type: application/sdp
Content-Length: [len]

v=0
o=bob 53655765 2353687638 IN IP[local_ip_type] [local_ip]
s=-
c=IN IP[media_ip_type] [media_ip]
t=0 0
m=audio [media_port] RTP/AVP 0 8 101
a=rtpmap:0 PCMU/8000
a=rtpmap:8 PCMA/8000
a=rtpmap:101 telephone-event/8000
a=fmtp:101 0-15
a=sendrecv
]]>
  </send>

  <recv request="ACK" optional="true" timeout="5000" />

  <!-- Receive BYE -->
  <recv request="BYE" />

  <!-- Send 200 OK for BYE -->
  <send>
    <![CDATA[
SIP/2.0 200 OK
[last_Via:]
[last_From:]
[last_To:]
[last_Call-ID:]
[last_CSeq:]
Content-Length: 0

]]>
  </send>

</scenario>
