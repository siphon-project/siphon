<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!--
  B2BUA UPDATE UAC scenario (RFC 3311):
  INVITE → 200 → ACK
  → UPDATE (empty body — RFC 4028 session-timer refresh style) → 200
  → UPDATE (with SDP — codec renegotiation) → 200
  → BYE → 200

  Validates that the B2BUA bridges in-dialog UPDATE end-to-end:
    * empty-body UPDATE (header-only refresh) reaches the B-leg and the
      response is forwarded back
    * SDP-bearing UPDATE reaches the B-leg, rtpengine is consulted, the
      response is bridged
    * 100 Trying suppresses T1-backoff retransmits
-->
<scenario name="B2BUA UPDATE UAC">

  <!-- Initial INVITE -->
  <send retrans="500">
    <![CDATA[
INVITE sip:bob@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:alice@[local_ip]>;tag=[pid]SIPpTag00[call_number]
To: <sip:bob@[remote_ip]>
Call-ID: [call_id]
CSeq: 1 INVITE
Contact: <sip:alice@[local_ip]:[local_port];transport=[transport]>
Max-Forwards: 70
Supported: timer
User-Agent: SIPp/b2bua-update-test
Content-Type: application/sdp
Content-Length: [len]

v=0
o=alice 53655765 2353687637 IN IP[local_ip_type] [local_ip]
s=-
c=IN IP[media_ip_type] [media_ip]
t=0 0
m=audio [media_port] RTP/AVP 0 8 101
a=rtpmap:0 PCMU/8000
a=rtpmap:8 PCMA/8000
a=rtpmap:101 telephone-event/8000
a=fmtp:101 0-15
a=sendrecv
]]>
  </send>

  <recv response="100" optional="true" />

  <label id="1" />
  <recv response="180" optional="true" next="1" />

  <recv response="200" rtd="true" crlf="true" />

  <send>
    <![CDATA[
ACK sip:bob@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:alice@[local_ip]>;tag=[pid]SIPpTag00[call_number]
To: <sip:bob@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 1 ACK
Max-Forwards: 70
Content-Length: 0

]]>
  </send>

  <pause milliseconds="500" />

  <!-- ============================================================ -->
  <!-- UPDATE #1: session-timer refresh style (no body)             -->
  <!-- ============================================================ -->
  <send retrans="500">
    <![CDATA[
UPDATE sip:bob@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:alice@[local_ip]>;tag=[pid]SIPpTag00[call_number]
To: <sip:bob@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 2 UPDATE
Contact: <sip:alice@[local_ip]:[local_port];transport=[transport]>
Max-Forwards: 70
Supported: timer
Session-Expires: 90;refresher=uac
User-Agent: SIPp/b2bua-update-test
Content-Length: 0

]]>
  </send>

  <recv response="100" optional="true" />

  <recv response="200" rtd="true" crlf="true" />

  <pause milliseconds="500" />

  <!-- ============================================================ -->
  <!-- UPDATE #2: codec renegotiation (with SDP)                    -->
  <!-- ============================================================ -->
  <send retrans="500">
    <![CDATA[
UPDATE sip:bob@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:alice@[local_ip]>;tag=[pid]SIPpTag00[call_number]
To: <sip:bob@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 3 UPDATE
Contact: <sip:alice@[local_ip]:[local_port];transport=[transport]>
Max-Forwards: 70
User-Agent: SIPp/b2bua-update-test
Content-Type: application/sdp
Content-Length: [len]

v=0
o=alice 53655765 2353687638 IN IP[local_ip_type] [local_ip]
s=-
c=IN IP[media_ip_type] [media_ip]
t=0 0
m=audio [media_port] RTP/AVP 8 101
a=rtpmap:8 PCMA/8000
a=rtpmap:101 telephone-event/8000
a=fmtp:101 0-15
a=sendrecv
]]>
  </send>

  <recv response="100" optional="true" />

  <recv response="200" rtd="true" crlf="true" />

  <pause milliseconds="200" />

  <!-- BYE -->
  <send retrans="500">
    <![CDATA[
BYE sip:bob@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:alice@[local_ip]>;tag=[pid]SIPpTag00[call_number]
To: <sip:bob@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 4 BYE
Max-Forwards: 70
Content-Length: 0

]]>
  </send>

  <recv response="200" rtd="true" />

</scenario>
