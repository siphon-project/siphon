<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!--
  B2BUA topology hiding UAC scenario:
  INVITE with high CSeq (99) and Max-Forwards (65) → 200 → ACK
  → re-INVITE (CSeq 100) → 200 → ACK → BYE (CSeq 101) → 200

  Tests that the B2BUA:
  - Regenerates CSeq for the B-leg (should NOT pass through 99/100/101)
  - Decrements Max-Forwards (should NOT pass through 65)
  - Rewrites From URI host (should NOT leak UAC's IP to B-leg)
  - Preserves user part in RURI (bob@ must appear in B-leg INVITE)
  - Rewrites To header host to dial target (not A-leg advertised address)
  - Uses Contact from 200 OK as ACK RURI
  - Uses remote_contact for re-INVITE and BYE RURI

  Also validates responses back to A-leg:
  - Contact in 200 OK points to siphon (172.20.0.50), not B-leg (172.20.0.52)
  - No B-leg topology leaks in response headers

  The UAS scenario validates these on the B-leg side.
-->
<scenario name="B2BUA topology hiding UAC">

  <!-- Send initial INVITE with distinctive CSeq and Max-Forwards -->
  <send retrans="500">
    <![CDATA[
INVITE sip:bob@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:alice@[local_ip]>;tag=[pid]SIPpTag00[call_number]
To: <sip:bob@[remote_ip]>
Call-ID: [call_id]
CSeq: 99 INVITE
Contact: <sip:alice@[local_ip]:[local_port];transport=[transport]>
Max-Forwards: 65
User-Agent: SIPp/topology-test
P-Asserted-Identity: <sip:alice@10.99.99.99>
Content-Type: application/sdp
Content-Length: [len]

v=0
o=LeakyUA 53655765 2353687637 IN IP[local_ip_type] [local_ip]
s=LeakyUA
c=IN IP[media_ip_type] [media_ip]
t=0 0
m=audio [media_port] RTP/AVP 0 8 101
a=rtpmap:0 PCMU/8000
a=rtpmap:8 PCMA/8000
a=rtpmap:101 telephone-event/8000
a=fmtp:101 0-15
a=sendrecv
]]>
  </send>

  <!-- Absorb provisionals -->
  <recv response="100" optional="true" />

  <label id="1" />
  <recv response="180" optional="true" next="1" />

  <!-- 200 OK for initial INVITE — validate Contact points to siphon, not B-leg -->
  <recv response="200" rtd="true" crlf="true">
    <action>
      <!-- Contact should be siphon's address (172.20.0.50), NOT the B-leg UAS (172.20.0.52) -->
      <ereg regexp="Contact:.*172\.20\.0\.50" search_in="msg" assign_to="contact_ok" />
      <!-- B-leg UAS IP should NOT appear in response -->
      <ereg regexp="172\.20\.0\.52" search_in="msg" assign_to="bleg_leak" />
    </action>
  </recv>

  <!-- ACK -->
  <send>
    <![CDATA[
ACK sip:bob@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:alice@[local_ip]>;tag=[pid]SIPpTag00[call_number]
To: <sip:bob@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 99 ACK
Max-Forwards: 70
Content-Length: 0

]]>
  </send>

  <pause milliseconds="500" />

  <!-- re-INVITE with CSeq 100 -->
  <send retrans="500">
    <![CDATA[
INVITE sip:bob@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:alice@[local_ip]>;tag=[pid]SIPpTag00[call_number]
To: <sip:bob@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 100 INVITE
Contact: <sip:alice@[local_ip]:[local_port];transport=[transport]>
Max-Forwards: 65
Content-Type: application/sdp
Content-Length: [len]

v=0
o=LeakyUA 53655765 2353687638 IN IP[local_ip_type] [local_ip]
s=LeakyUA
c=IN IP[media_ip_type] [media_ip]
t=0 0
m=audio [media_port] RTP/AVP 0 8 101
a=rtpmap:0 PCMU/8000
a=rtpmap:8 PCMA/8000
a=rtpmap:101 telephone-event/8000
a=fmtp:101 0-15
a=sendrecv
]]>
  </send>

  <recv response="100" optional="true" />
  <recv response="200" rtd="true" crlf="true" />

  <send>
    <![CDATA[
ACK sip:bob@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:alice@[local_ip]>;tag=[pid]SIPpTag00[call_number]
To: <sip:bob@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 100 ACK
Max-Forwards: 70
Content-Length: 0

]]>
  </send>

  <pause milliseconds="200" />

  <!-- BYE with CSeq 101 -->
  <send retrans="500">
    <![CDATA[
BYE sip:bob@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:alice@[local_ip]>;tag=[pid]SIPpTag00[call_number]
To: <sip:bob@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 101 BYE
Max-Forwards: 65
Content-Length: 0

]]>
  </send>

  <recv response="200" rtd="true" />

</scenario>
