<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!--
  B2BUA CANCEL UAC scenario:
  INVITE → provisionals → CANCEL → 200 (CANCEL) → 487 (INVITE) → ACK

  Tests that the B2BUA correctly propagates CANCEL to the B-leg
  and returns 487 Request Terminated to the A-leg.

  Note: We wait for 180 Ringing before sending CANCEL.
  CANCEL before any provisional is invalid per RFC 3261 §9.1.
-->
<scenario name="B2BUA CANCEL UAC">

  <!-- Send INVITE to B2BUA -->
  <send retrans="500">
    <![CDATA[
INVITE sip:bob@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:alice@[local_ip]>;tag=[pid]SIPpTag00[call_number]
To: <sip:bob@[remote_ip]>
Call-ID: [call_id]
CSeq: 1 INVITE
Contact: <sip:alice@[local_ip]:[local_port];transport=[transport]>
Max-Forwards: 70
User-Agent: SIPp/b2bua-cancel-test
Content-Type: application/sdp
Content-Length: [len]

v=0
o=alice 53655765 2353687637 IN IP[local_ip_type] [local_ip]
s=-
c=IN IP[media_ip_type] [media_ip]
t=0 0
m=audio [media_port] RTP/AVP 0 8 101
a=rtpmap:0 PCMU/8000
a=rtpmap:8 PCMA/8000
a=rtpmap:101 telephone-event/8000
a=fmtp:101 0-15
]]>
  </send>

  <!-- Absorb 100 Trying -->
  <recv response="100" optional="true" />

  <!-- Wait for 180 Ringing (mandatory — CANCEL is only valid after a provisional) -->
  <recv response="180" />

  <!-- Send CANCEL -->
  <send>
    <![CDATA[
CANCEL sip:bob@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:alice@[local_ip]>;tag=[pid]SIPpTag00[call_number]
To: <sip:bob@[remote_ip]>
Call-ID: [call_id]
CSeq: 1 CANCEL
Max-Forwards: 70
Content-Length: 0

]]>
  </send>

  <!-- Expect 200 OK for the CANCEL -->
  <recv response="200" rtd="true" />

  <!-- Expect 487 Request Terminated for the original INVITE -->
  <recv response="487" />

  <!-- Send ACK for the 487 -->
  <send>
    <![CDATA[
ACK sip:bob@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:alice@[local_ip]>;tag=[pid]SIPpTag00[call_number]
To: <sip:bob@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 1 ACK
Max-Forwards: 70
Content-Length: 0

]]>
  </send>

</scenario>
