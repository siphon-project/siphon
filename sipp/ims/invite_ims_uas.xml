<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!--
  IMS MTC (Mobile Terminated Call) UAS scenario.

  bob receives an INVITE relayed by the S-CSCF, answers, waits for BYE.
  Handles ACK retransmissions (absorbed via optional recv loop).

  Use with:
    sipp -sf invite_ims_uas.xml -p 5060 -m 1
-->
<scenario name="IMS MTC: bob receives call">

  <!-- Receive INVITE from S-CSCF -->
  <recv request="INVITE" crlf="true" />

  <!-- Send 180 Ringing -->
  <send>
    <![CDATA[
SIP/2.0 180 Ringing
[last_Via:]
[last_Record-Route:]
[last_From:]
[last_To:];tag=[pid]SIPpTag01[call_number]
[last_Call-ID:]
[last_CSeq:]
Contact: <sip:bob@[local_ip]:[local_port];transport=[transport]>
Content-Length: 0

]]>
  </send>

  <!-- Brief pause before answering -->
  <pause milliseconds="500" />

  <!-- Send 200 OK with SDP -->
  <send retrans="500">
    <![CDATA[
SIP/2.0 200 OK
[last_Via:]
[last_Record-Route:]
[last_From:]
[last_To:];tag=[pid]SIPpTag01[call_number]
[last_Call-ID:]
[last_CSeq:]
Contact: <sip:bob@[local_ip]:[local_port];transport=[transport]>
Content-Type: application/sdp
Content-Length: [len]

v=0
o=bob 53655765 2353687637 IN IP[local_ip_type] [local_ip]
s=-
c=IN IP[media_ip_type] [media_ip]
t=0 0
m=audio [media_port] RTP/AVP 0 8 101
a=rtpmap:0 PCMU/8000
a=rtpmap:8 PCMA/8000
a=rtpmap:101 telephone-event/8000
a=fmtp:101 0-15
]]>
  </send>

  <!-- Receive ACK (absorb retransmissions via optional loop) -->
  <recv request="ACK" />
  <label id="1" />
  <recv request="ACK" optional="true" next="1" />

  <!-- Receive BYE -->
  <recv request="BYE" />

  <!-- Send 200 OK for BYE -->
  <send>
    <![CDATA[
SIP/2.0 200 OK
[last_Via:]
[last_From:]
[last_To:]
[last_Call-ID:]
[last_CSeq:]
Content-Length: 0

]]>
  </send>

</scenario>
